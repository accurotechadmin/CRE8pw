# Onboarding Session Notes — 2026-05-13

## Scope completed
- Processed the mandatory sequence in `dev/CRE8_ONBOARDING_COMPREHENSIVE_READING_LIST.md` (items 1–124) in order.
- Reconfirmed repository-wide source existence for all listed files (`count=124`, `missing=0`).

## Source authority map
1. `docs/` is normative SSOT unless governance explicitly promotes content elsewhere.
2. `seed/` is ethos/provenance and input corpus for promotion, not default runtime authority.
3. `reports/` and `dev/implementation/` are continuity and execution artifacts unless explicitly promoted by governance.

## Implementation posture
- Treat SSOT contradictions as merge-blocking pending reconciliation via governance + ADR flow.
- Preserve requirement IDs / hook IDs / doc IDs in design and code discussions.
- Maintain prose ↔ OpenAPI ↔ schema parity and evidence-ready traceability.

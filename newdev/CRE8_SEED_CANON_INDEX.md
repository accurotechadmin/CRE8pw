# CRE8 Seed Canon Index (`/newdev`)

This index defines the initial authoritative document map for the next CRE8 SSOT lifecycle. `CRE8_KEYPAIR_MODEL_BASE_INVENTORY.md` is the root inventory and all other seed docs elaborate specific domains (permissions, lifecycle, security, surfaces, extensibility) without contradicting it.

As this set matures, each seed file should evolve into a formal canonical spec with contracts, decision tables, examples, and verification criteria. The expected governance posture remains contract-first, policy-first, deterministic, and traceable.

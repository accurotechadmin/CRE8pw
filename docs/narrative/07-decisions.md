# 07 Decisions

_Documentary script chunks following the recommended reading order._

## Chapter 43: `docs/ssot_canon/60_decisions/ADR_INDEX.md`

In Chapter 43, "ADR Index" becomes a camera move across one specific CRE8 layer: `docs/ssot_canon/60_decisions/ADR_INDEX.md`. The document frames how this layer operates through Purpose, Current indexed records, Index contract. This chapter is treated as a system node that either feeds standards downward into implementation or pulls evidence upward into governance.

The sub-components in this chapter are interpreted as operational actors—owners, interfaces, data shapes, runtime behaviors, and release gates—that must synchronize with adjacent chapters to keep CRE8 coherent end-to-end. When this chapter defines constraints, other chapters inherit them; when it defines outputs, downstream contracts, tests, and evidence artifacts consume them.

Narratively, the document opens with context like: "Provide discoverable index of architectural and policy decisions that materially affect implementation behavior.". That framing is used as the voice-over baseline for explaining why the chapter exists and what coupling points it introduces into the wider platform story.

## Chapter 44: `docs/ssot_canon/60_decisions/DECISIONS_LOG.md`

In Chapter 44, "Decisions Log" becomes a camera move across one specific CRE8 layer: `docs/ssot_canon/60_decisions/DECISIONS_LOG.md`. The document frames how this layer operates through Chronological entries, Update rule. This chapter is treated as a system node that either feeds standards downward into implementation or pulls evidence upward into governance.

The sub-components in this chapter are interpreted as operational actors—owners, interfaces, data shapes, runtime behaviors, and release gates—that must synchronize with adjacent chapters to keep CRE8 coherent end-to-end. When this chapter defines constraints, other chapters inherit them; when it defines outputs, downstream contracts, tests, and evidence artifacts consume them.

Narratively, the document opens with context like: "Any decision that changes API shape, auth policy, data invariants, or release gates must be logged here in same PR.". That framing is used as the voice-over baseline for explaining why the chapter exists and what coupling points it introduces into the wider platform story.

## Chapter 45: `docs/ssot_canon/60_decisions/records/ADR-001-ssot-first-governance.md`

In Chapter 45, "ADR-001: SSOT-first contract governance model" becomes a camera move across one specific CRE8 layer: `docs/ssot_canon/60_decisions/records/ADR-001-ssot-first-governance.md`. The document frames how this layer operates through Context, Decision, Consequences, Verification implications. This chapter is treated as a system node that either feeds standards downward into implementation or pulls evidence upward into governance.

The sub-components in this chapter are interpreted as operational actors—owners, interfaces, data shapes, runtime behaviors, and release gates—that must synchronize with adjacent chapters to keep CRE8 coherent end-to-end. When this chapter defines constraints, other chapters inherit them; when it defines outputs, downstream contracts, tests, and evidence artifacts consume them.

Narratively, the document opens with context like: "Historically, behavioral truth was split across prose docs and implementation assumptions.". That framing is used as the voice-over baseline for explaining why the chapter exists and what coupling points it introduces into the wider platform story.

## Chapter 46: `docs/ssot_canon/60_decisions/records/ADR-002-delegation-envelope-bounds.md`

In Chapter 46, "ADR-002: Delegation envelope bounds (subset/depth/expiry)" becomes a camera move across one specific CRE8 layer: `docs/ssot_canon/60_decisions/records/ADR-002-delegation-envelope-bounds.md`. The document frames how this layer operates through Context, Decision, Consequences, Verification implications. This chapter is treated as a system node that either feeds standards downward into implementation or pulls evidence upward into governance.

The sub-components in this chapter are interpreted as operational actors—owners, interfaces, data shapes, runtime behaviors, and release gates—that must synchronize with adjacent chapters to keep CRE8 coherent end-to-end. When this chapter defines constraints, other chapters inherit them; when it defines outputs, downstream contracts, tests, and evidence artifacts consume them.

Narratively, the document opens with context like: "Delegated key issuance required deterministic safety boundaries to prevent privilege escalation.". That framing is used as the voice-over baseline for explaining why the chapter exists and what coupling points it introduces into the wider platform story.

## Chapter 47: `docs/ssot_canon/60_decisions/records/ADR-003-keychain-production-principal.md`

In Chapter 47, "ADR-003: Keychain as production-active v1 principal class" becomes a camera move across one specific CRE8 layer: `docs/ssot_canon/60_decisions/records/ADR-003-keychain-production-principal.md`. The document frames how this layer operates through Context, Decision, Consequences, Verification implications. This chapter is treated as a system node that either feeds standards downward into implementation or pulls evidence upward into governance.

The sub-components in this chapter are interpreted as operational actors—owners, interfaces, data shapes, runtime behaviors, and release gates—that must synchronize with adjacent chapters to keep CRE8 coherent end-to-end. When this chapter defines constraints, other chapters inherit them; when it defines outputs, downstream contracts, tests, and evidence artifacts consume them.

Narratively, the document opens with context like: "Collaborative authoring needed group-based authority while preserving lineage and policy control.". That framing is used as the voice-over baseline for explaining why the chapter exists and what coupling points it introduces into the wider platform story.

## Chapter 48: `docs/ssot_canon/60_decisions/records/ADR-004-envelope-first-api-standard.md`

In Chapter 48, "ADR-004: Envelope-first API response standard" becomes a camera move across one specific CRE8 layer: `docs/ssot_canon/60_decisions/records/ADR-004-envelope-first-api-standard.md`. The document frames how this layer operates through Context, Decision, Consequences, Verification implications. This chapter is treated as a system node that either feeds standards downward into implementation or pulls evidence upward into governance.

The sub-components in this chapter are interpreted as operational actors—owners, interfaces, data shapes, runtime behaviors, and release gates—that must synchronize with adjacent chapters to keep CRE8 coherent end-to-end. When this chapter defines constraints, other chapters inherit them; when it defines outputs, downstream contracts, tests, and evidence artifacts consume them.

Narratively, the document opens with context like: "Inconsistent API payload structures increased frontend and observability complexity.". That framing is used as the voice-over baseline for explaining why the chapter exists and what coupling points it introduces into the wider platform story.

## Chapter 49: `docs/ssot_canon/60_decisions/records/ADR-005-release-gating-controls.md`

In Chapter 49, "ADR-005: Release gating via verification + smoke + readiness controls" becomes a camera move across one specific CRE8 layer: `docs/ssot_canon/60_decisions/records/ADR-005-release-gating-controls.md`. The document frames how this layer operates through Context, Decision, Consequences, Verification implications. This chapter is treated as a system node that either feeds standards downward into implementation or pulls evidence upward into governance.

The sub-components in this chapter are interpreted as operational actors—owners, interfaces, data shapes, runtime behaviors, and release gates—that must synchronize with adjacent chapters to keep CRE8 coherent end-to-end. When this chapter defines constraints, other chapters inherit them; when it defines outputs, downstream contracts, tests, and evidence artifacts consume them.

Narratively, the document opens with context like: "Passing unit tests alone was insufficient to ensure production readiness and operational confidence.". That framing is used as the voice-over baseline for explaining why the chapter exists and what coupling points it introduces into the wider platform story.

## Chapter 50: `docs/ssot_canon/60_decisions/DECISION_RECORD_TEMPLATE.md`

In Chapter 50, "Decision Record Template" becomes a camera move across one specific CRE8 layer: `docs/ssot_canon/60_decisions/DECISION_RECORD_TEMPLATE.md`. The document frames how this layer operates through Required fields, Quality bar. This chapter is treated as a system node that either feeds standards downward into implementation or pulls evidence upward into governance.

The sub-components in this chapter are interpreted as operational actors—owners, interfaces, data shapes, runtime behaviors, and release gates—that must synchronize with adjacent chapters to keep CRE8 coherent end-to-end. When this chapter defines constraints, other chapters inherit them; when it defines outputs, downstream contracts, tests, and evidence artifacts consume them.

Narratively, the document opens with context like: "A decision record is invalid if it lacks either explicit consequences or verification implications.". That framing is used as the voice-over baseline for explaining why the chapter exists and what coupling points it introduces into the wider platform story.


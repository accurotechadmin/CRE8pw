# 09 Program Management

_Documentary script chunks following the recommended reading order._

## Chapter 55: `docs/ssot_canon/80_program_management/ROADMAP_AND_MILESTONES.md`

In Chapter 55, "Roadmap and Milestones" becomes a camera move across one specific CRE8 layer: `docs/ssot_canon/80_program_management/ROADMAP_AND_MILESTONES.md`. The document frames how this layer operates through Milestones, Exit criteria by milestone, Milestone deliverables (minimum), Tracking rule. This chapter is treated as a system node that either feeds standards downward into implementation or pulls evidence upward into governance.

The sub-components in this chapter are interpreted as operational actors—owners, interfaces, data shapes, runtime behaviors, and release gates—that must synchronize with adjacent chapters to keep CRE8 coherent end-to-end. When this chapter defines constraints, other chapters inherit them; when it defines outputs, downstream contracts, tests, and evidence artifacts consume them.

Narratively, the document opens with context like: "Each milestone closeout must include: (1) evidence template, (2) traceability diff, (3) decision-log updates for policy changes.". That framing is used as the voice-over baseline for explaining why the chapter exists and what coupling points it introduces into the wider platform story.

## Chapter 56: `docs/ssot_canon/80_program_management/RISK_REGISTER.md`

In Chapter 56, "Risk Register" becomes a camera move across one specific CRE8 layer: `docs/ssot_canon/80_program_management/RISK_REGISTER.md`. The document frames how this layer operates through Active risks, Review cadence. This chapter is treated as a system node that either feeds standards downward into implementation or pulls evidence upward into governance.

The sub-components in this chapter are interpreted as operational actors—owners, interfaces, data shapes, runtime behaviors, and release gates—that must synchronize with adjacent chapters to keep CRE8 coherent end-to-end. When this chapter defines constraints, other chapters inherit them; when it defines outputs, downstream contracts, tests, and evidence artifacts consume them.

Narratively, this chapter is presented as connective tissue: it translates policy into executable behavior and turns implementation facts into auditable evidence.

## Chapter 57: `docs/ssot_canon/80_program_management/CONTRIBUTION_WORKFLOW_SSOT.md`

In Chapter 57, "Contribution Workflow (SSOT)" becomes a camera move across one specific CRE8 layer: `docs/ssot_canon/80_program_management/CONTRIBUTION_WORKFLOW_SSOT.md`. The document frames how this layer operates through Workflow, Required PR payload, Review policy, SLA and escalation. This chapter is treated as a system node that either feeds standards downward into implementation or pulls evidence upward into governance.

The sub-components in this chapter are interpreted as operational actors—owners, interfaces, data shapes, runtime behaviors, and release gates—that must synchronize with adjacent chapters to keep CRE8 coherent end-to-end. When this chapter defines constraints, other chapters inherit them; when it defines outputs, downstream contracts, tests, and evidence artifacts consume them.

Narratively, the document opens with context like: "1. Identify changed capability and enumerate impacted SSOT artifacts (contracts, machine artifacts, data/security, operations, traceability). 2. Update normative docs first (`20_`, `30_`, `40_` families) before implementation PR merge. 3. Update machine artifa". That framing is used as the voice-over baseline for explaining why the chapter exists and what coupling points it introduces into the wider platform story.

## Chapter 58: `docs/ssot_canon/80_program_management/DEFINITION_OF_DONE.md`

In Chapter 58, "Definition of Done" becomes a camera move across one specific CRE8 layer: `docs/ssot_canon/80_program_management/DEFINITION_OF_DONE.md`. The document frames how this layer operates through Done means all are true, Required evidence checklist, Not-done examples. This chapter is treated as a system node that either feeds standards downward into implementation or pulls evidence upward into governance.

The sub-components in this chapter are interpreted as operational actors—owners, interfaces, data shapes, runtime behaviors, and release gates—that must synchronize with adjacent chapters to keep CRE8 coherent end-to-end. When this chapter defines constraints, other chapters inherit them; when it defines outputs, downstream contracts, tests, and evidence artifacts consume them.

Narratively, this chapter is presented as connective tissue: it translates policy into executable behavior and turns implementation facts into auditable evidence.


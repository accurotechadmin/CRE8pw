# P4-S2.5 — Policy Signal Precedence Closure

- Added `CRE8-AUTH-REQ-0007` in `AUTHORIZATION_AND_DELEGATION_SPEC.md`.
- Precedence order is now explicit and deterministic: `explicit_deny > scope_constraint_deny > permission_missing_deny > delegated_allow > direct_allow`.
- Added direct-vs-delegated allow provenance requirement (`allow_source=direct`).
